{#
   Field Label
   Outputs a field label with the correct markup

   @type {include}
   @param {string} id
   @param {string} label
   @param {string} class
   @param {bool} required
#}

{% set baseClass = 'block font-extrabold text-base mb-1 text-black' %}

{% set class = class is defined ? baseClass ~ ' ' ~ class : baseClass %}

<label for="{{ id }}" class="{{ class }}">
   {{ label }}
   {% if required is defined and required %}
      <span class="text-black ml-px">*</span>
   {% endif %}
</label>
